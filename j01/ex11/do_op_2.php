#!/usr/bin/php
<?php

function epur($elem)
if ($elem == 0)
	return(0);
$f = explode(" ", $elem);
$f = array_filter($f);
$f = implode(" ", $f);
return(f);


if ($argc != 2)
{
	echo "Incorrect Parameters\n";
	return(0);
}

echo "$res\n";
?>